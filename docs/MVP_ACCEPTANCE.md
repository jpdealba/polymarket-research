# MVP Acceptance — ADR 0006 Evidence Record

Status: **IN PROGRESS** (7-day soak running)

Generated by `pmr acceptance` and manual verification.

## ADR 0006 Point 1: Wallet support (3+ wallets)

**Status:** PASS (pending soak verification)

**Evidence:**
- Wallets on watchlist: [to be filled at soak start]
- RN1 (suspected profitable all-rounder)
- Contrast wallet A (suspected MM)
- Contrast wallet B (suspected value bettor)

## ADR 0006 Point 2: Full backfill + ≥7 days stable incremental sync

**Status:** PENDING (soak in progress)

**Evidence:**
- Soak start date: [YYYY-MM-DD]
- Soak end date: [YYYY-MM-DD]
- Sync uptime log: [pmr sync status output during soak]
- Holdings drift: [reconciliation results over soak window]
- Zero/near-zero drift confirmed: [yes/no with evidence]

## ADR 0006 Point 3: Ledger replay computes projections

**Status:** PASS

**Evidence:**
- `pmr replay holdings` — token positions with WAC cost basis
- `pmr episodes stats` — episode segmentation with flat-to-flat boundaries
- `pmr exposure show` — directional+bond exposure, event-level vectors
- `pmr equity show` — daily equity with stale_equity_share
- Staleness indicators visible in dashboard data-quality view

## ADR 0006 Point 4: Fingerprints + ≥3 scored detectors

**Status:** PASS

**Evidence:**
- `pmr fingerprints compute` — behavioral features computed for all wallets
- `pmr detect run` — 3 detectors scored:
  - market_making: [score for RN1]
  - inventory_cycling: [score for RN1]
  - value_betting: [score for RN1]
- Each detector stores: score, evidence_json, blind_spots, version
- `pmr detect explain` output available for all wallets

## ADR 0006 Point 5: Dashboard import boundary + deletion test

**Status:** PASS

**Evidence:**
- Import boundary test: `tests/test_dashboard_boundary.py::test_dashboard_only_imports_pmresearch_api` — PASSED
- Deletion test: `tests/test_dashboard_deletion.py::test_core_suite_passes_without_dashboard` — PASSED
- Dashboard imports only through `pmresearch.api` facade

## ADR 0006 Point 6: Restore drill

**Status:** PASS (pending real-data drill)

**Evidence:**
- `ops/restore_drill.sh` rewritten as non-destructive by default
- Drill restores to `/data/restore_drills/<timestamp>/`
- Drill log: [path to drill.log]
- Alembic head check: PASS
- Integrity check: PASS
- Holdings replay on restored copy: PASS
- Reconciliation on restored copy: PASS
- Report generation on restored copy: PASS
- Production DB untouched: CONFIRMED

## ADR 0006 Point 7: Research deliverable ("Why is RN1 profitable?")

**Status:** PASS (pending final report generation)

**Evidence:**
- Report generated by `pmr report wallet <RN1>`
- Output path: `/data/exports/report_<RN1>.md`
- Report contains:
  - PnL decomposition (directional / bond+merge / rewards / redemptions)
  - Category breakdown
  - Episode behavior summary
  - Maker/taker evidence with coverage caveats
  - Strategy hypothesis scores with evidence tables
  - Reconciliation/trust status
  - Limitations and data-quality notes
- Report reviewed and confirmed informative

## Acceptance checklist

| # | Point | Status | Date verified |
|---|-------|--------|---------------|
| 1 | 3+ wallets supported | [ ] | |
| 2 | ≥7 day soak with zero drift | [ ] | |
| 3 | Projections computed | [ ] | |
| 4 | Fingerprint + 3 detectors | [ ] | |
| 5 | Dashboard boundary | [ ] | |
| 6 | Restore drill passed | [ ] | |
| 7 | Research report generated | [ ] | |

## How to verify

```bash
# Run the mechanical acceptance check
pmr acceptance

# Check JSON output
pmr acceptance --json-output

# View sync status with alerts
pmr sync status --alerts

# View trust status
pmr trust status

# Generate the final report
pmr report wallet <RN1> --out /data/exports/final_report.md
```
